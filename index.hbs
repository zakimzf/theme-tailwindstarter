{{!< default}}

<section class="my-24 text-center">
<h1 class="text-4xl">
    {{@site.title}}
</h1>
<p class="text-2xl">{{@site.description}}</p>
</section>

<section class="space-y-8 mb-8">
{{#foreach posts visibility="all"}}

    {{> "card"}} {{!-- partials/card.hbs --}}

{{/foreach}}
</section>
